# Optional. Without a key, image analysis returns a safe demo fallback.
GEMINI_API_KEY=

# Optional persistent database path.
CHOREMAP_DB_PATH=choremap.db

# Required to unlock the unlisted /analytics owner dashboard.
# Use a long password and configure it privately on the hosting service.
ANALYTICS_PASSWORD=
